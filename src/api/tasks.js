export default {
  fetch () {
    // axios.get('api/v1/tasks').then(response => {
    //   let tasks = response.data
    //   context.commit('tasks', tasks)
    // }).catch(error => {
    //   console.log(error)
    // })
    return [
      {'name': 'C'},
      {'name': 'E'},
      {'name': 'T'}
    ]
  }
}
